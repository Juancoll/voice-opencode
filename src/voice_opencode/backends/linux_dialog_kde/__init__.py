"""KDE-flavoured dialog/notify backends (kdialog + notify-send).

Despite the package name, ``knotify_backend.LibnotifyBackend`` is
desktop-agnostic — every modern Linux DE ships a libnotify daemon.
"""
